import { VirtualFile } from '../types';
import { SKILLS_DATA } from './skillsData';

export function generateVirtualFileTree(): VirtualFile[] {
  return [
    {
      id: 'root-skills',
      name: 'skills',
      path: '/skills',
      type: 'folder',
      children: SKILLS_DATA.map((skill) => ({
        id: `folder-${skill.id}`,
        name: `${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}`,
        path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}`,
        type: 'folder',
        category: skill.category,
        children: [
          {
            id: `file-${skill.id}-skillmd`,
            name: 'SKILL.md',
            path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/SKILL.md`,
            type: 'file',
            extension: 'md',
            sizeKb: 4.2,
            content: skill.skillMdContent || `# Skill: ${skill.name} (${skill.number})\n\n## Purpose\n${skill.purpose}\n\n## Category: ${skill.category}\n\n## Inputs\n${skill.requiredInputs.map(i => `- ${i}`).join('\n')}\n\n## Outputs\n${skill.outputs.map(o => `- ${o}`).join('\n')}`,
          },
          {
            id: `folder-${skill.id}-schemas`,
            name: 'schemas',
            path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/schemas`,
            type: 'folder',
            children: [
              {
                id: `file-${skill.id}-schema-main`,
                name: 'schema.json',
                path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/schemas/schema.json`,
                type: 'file',
                extension: 'json',
                sizeKb: 2.8,
                content: skill.schemaPreview || `{\n  "$schema": "https://json-schema.org/draft/2020-12/schema",\n  "title": "${skill.name}Spec",\n  "type": "object",\n  "version": "${skill.version}"\n}`,
              },
              {
                id: `file-${skill.id}-schema-config`,
                name: 'config.schema.json',
                path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/schemas/config.schema.json`,
                type: 'file',
                extension: 'json',
                sizeKb: 1.6,
                content: `{\n  "title": "${skill.name}RuntimeConfig",\n  "type": "object",\n  "properties": {\n    "concurrency": { "type": "integer", "default": 8 },\n    "timeoutMs": { "type": "integer", "default": 5000 }\n  }\n}`,
              },
            ],
          },
          {
            id: `folder-${skill.id}-prompts`,
            name: 'prompts',
            path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/prompts`,
            type: 'folder',
            children: [
              {
                id: `file-${skill.id}-prompt-sys`,
                name: 'system.prompt.md',
                path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/prompts/system.prompt.md`,
                type: 'file',
                extension: 'md',
                sizeKb: 3.1,
                content: skill.promptPreview || `You are an expert specialist in ${skill.name}.\nExecute the ${skill.purpose} with canonical standard verification.`,
              },
              {
                id: `file-${skill.id}-prompt-eval`,
                name: 'evaluation.prompt.md',
                path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/prompts/evaluation.prompt.md`,
                type: 'file',
                extension: 'md',
                sizeKb: 2.2,
                content: `Evaluate the deliverable quality against the ${skill.name} acceptance criteria. Ensure zero defects.`,
              },
              {
                id: `file-${skill.id}-prompt-fewshot`,
                name: 'few-shot.prompt.json',
                path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/prompts/few-shot.prompt.json`,
                type: 'file',
                extension: 'json',
                sizeKb: 5.4,
                content: `[\n  {\n    "input": "Sample query for ${skill.name}",\n    "output": "Canonical structured execution plan"\n  }\n]`,
              },
            ],
          },
          {
            id: `folder-${skill.id}-src`,
            name: 'src',
            path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/src`,
            type: 'folder',
            children: [
              {
                id: `file-${skill.id}-index`,
                name: 'index.ts',
                path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/src/index.ts`,
                type: 'file',
                extension: 'ts',
                sizeKb: 6.8,
                content: skill.sourcePreview || `/**\n * Canonical Implementation of ${skill.name} (v${skill.version})\n */\nexport interface ExecuteContext {\n  skillId: '${skill.id}';\n  inputs: Record<string, unknown>;\n}\n\nexport async function runSkill(ctx: ExecuteContext) {\n  // Implementation logic\n  return { success: true, timestamp: Date.now() };\n}`,
              },
              {
                id: `file-${skill.id}-types`,
                name: 'types.ts',
                path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/src/types.ts`,
                type: 'file',
                extension: 'ts',
                sizeKb: 3.4,
                content: `export type ${skill.name.replace(/[^a-zA-Z0-9]/g, '')}Status = 'active' | 'archived';\nexport interface ${skill.name.replace(/[^a-zA-Z0-9]/g, '')}Options {\n  strictMode?: boolean;\n}`,
              },
            ],
          },
          {
            id: `folder-${skill.id}-tests`,
            name: 'tests',
            path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/tests`,
            type: 'folder',
            children: [
              {
                id: `file-${skill.id}-spec`,
                name: `${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}.spec.ts`,
                path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/tests/${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}.spec.ts`,
                type: 'file',
                extension: 'ts',
                sizeKb: 4.5,
                content: skill.testPreview || `describe('${skill.name} (${skill.number})', () => {\n  it('should pass canonical schema validation', async () => {\n    expect(true).toBe(true);\n  });\n});`,
              },
              {
                id: `file-${skill.id}-integration-spec`,
                name: 'integration.spec.ts',
                path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/tests/integration.spec.ts`,
                type: 'file',
                extension: 'ts',
                sizeKb: 5.1,
                content: `describe('${skill.name} Integration Pipeline', () => {\n  it('should integrate seamlessly with dependencies', async () => {\n    expect(true).toBe(true);\n  });\n});`,
              },
            ],
          },
          {
            id: `folder-${skill.id}-examples`,
            name: 'examples',
            path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/examples`,
            type: 'folder',
            children: [
              {
                id: `file-${skill.id}-example-1`,
                name: 'basic-usage.ts',
                path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/examples/basic-usage.ts`,
                type: 'file',
                extension: 'ts',
                sizeKb: 2.1,
                content: `import { runSkill } from '../src';\n\nconst res = await runSkill({ skillId: '${skill.id}', inputs: {} });\nconsole.log(res);`,
              },
            ],
          },
          {
            id: `file-${skill.id}-changelog`,
            name: 'CHANGELOG.md',
            path: `/skills/${skill.number}-${skill.name.toLowerCase().replace(/[^a-z0-9]/g, '-')}/CHANGELOG.md`,
            type: 'file',
            extension: 'md',
            sizeKb: 1.8,
            content: `# Changelog - ${skill.name}\n\n## [${skill.version}] - ${skill.lastUpdated}\n- Upgraded schemas to Draft 2020-12\n- Enforced zero-trust validation\n- 100% test coverage verified`,
          },
        ],
      })),
    },
    {
      id: 'root-shared',
      name: 'shared',
      path: '/shared',
      type: 'folder',
      children: [
        {
          id: 'shared-registry-manifest',
          name: 'registry.manifest.json',
          path: '/shared/registry.manifest.json',
          type: 'file',
          extension: 'json',
          sizeKb: 14.5,
          content: JSON.stringify({ version: '1.0.0', totalSkills: 35, standard: 'Enterprise TSR v1' }, null, 2),
        },
        {
          id: 'shared-rbac-rules',
          name: 'rbac.rules.json',
          path: '/shared/rbac.rules.json',
          type: 'file',
          extension: 'json',
          sizeKb: 4.2,
          content: JSON.stringify({ roles: ['ADMIN', 'ARCHITECT', 'SECURITY_LEAD', 'VIEWER'] }, null, 2),
        },
      ],
    },
  ];
}
